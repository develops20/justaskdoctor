/**
 * Deliberately broken demo policy. JustAsk's registered patch raises this
 * limit to 180 in the in-memory demo runtime after explicit approval.
 */
export const MAX_ONLINE_DURATION_MINUTES = 90;
